# Backend

Thư mục dành cho mã nguồn backend của ứng dụng.

Chưa chọn framework ở giai đoạn chuẩn hóa repo. Khi triển khai, backend nên đọc schema từ `database/schema/unified_postgresql_schema.sql` và tuân thủ rule trong `knowledge_base/`.
